@model Financial_Management_Application.Models.CategoryVM.EditViewModel
@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>
@Html.ActionLink("View Products", "Index", "Product", null, new { @class = "btn" })
@Html.ActionLink("View Categories", "Index", "Category", null, new { @class = "btn" })
@using (Ajax.BeginForm("Edit", "Category", new AjaxOptions { HttpMethod = "POST" }, new { @class = "form-horizontal", role = "form" }))
{
    <div class="form-group">
        <label class="col-md-2 control-label">Name</label>
        <div class="col-md-10">
            @Html.TextBoxFor(m => m.category.name, new { @class = "form-control" })
        </div>
    </div>
    
    <div class="form-group">
        <div class="col-md-10">
            <input type="submit" class="btn" value="Save" />
            @Html.ActionLink("Cancel", "Index", "Category", new { @class = "btn btn-default" })
        </div>
    </div>
}
